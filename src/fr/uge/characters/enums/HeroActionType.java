package fr.uge.characters.enums;

/**
 * Enum listing the types of actions a hero can perform during a fight.
 */
public enum HeroActionType {
    /** Using a shield or defensive item. */
	PROTECTION,
    /** Attacking an enemy. */
	ATTACK
}